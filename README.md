# remo-web

REMO 팀 웹사이트. 배포: https://remo-web.vercel.app/ (2026-09-18 공개)

## 이 사이트가 하는 일

설득이 아니라 **필터**. 맞지 않는 방문자를 빠르게 내보내고, 맞는 방문자에게 확신을 준다.
성공 지표는 문의 수가 아니라 문의 대비 계약률.
기각 조건: 2026-12-31까지 유효 문의 3건 미만이면 이 전제를 재검토한다 (N=3은 임시값).

## 구조

- `site/` — 배포되는 정적 사이트 (GitHub Pages가 이 폴더만 본다)
- `docs/` — 구조(IA), 결정 기록, 섹션별 계획 (배포되지 않음)
- `.claude/skills/` — 팀이 공유하는 AI 디자인 스킬 + 리모 오버라이드
- `.github/workflows/pages.yml` — main 푸시 시 자동 배포

## 규칙을 쓰는 방식

이 문서에 "금지"는 없다. 모든 규칙은 **조건 + 이유 + 재검토 조건**으로 쓴다.
디자인 결정은 `docs/plan-*.md`에 **결정 / 이유 / 근거 / 기각안** 4칸으로 기록한다.

**결정 권한 순서** (위가 아래를 이긴다 — 2026-09-18, plan-09 §0):
1. 콘텐츠 진실 · 접근성(WCAG AA) · 개인정보 동의 게이트
2. 실측(실기기·명암비 계산·LCP)
3. 브랜드 가이드 v4 원문 (`docs/brand-guide-v4.md`)
4. 팀장 판단 — 근거 칸에 "취향"이라고 적는다
5. AI Council 결론 — 권고

**합격 기준**: 외부인 5명 10초 노출 → "무엇을 하는 팀" 정답 ≥3/5, 노드를 만진 사람 ≥3/5, "촌스럽다" 0/5.

## 디자인 기준 — 브랜드 가이드 v4 (2026-09-18 재설계 이후)

| 기준 | 이유 | 재검토 조건 |
|---|---|---|
| 팔레트는 잉크 `#0A0A0A` / 오션 블루 `#1E548C` / 근백색 `#F8F8F6`. **바탕은 교대한다** — 로고·선언·이탈 구간(히어로·방향·푸터)은 잉크, 장문 구간(포트폴리오·팀·문의)은 근백색 | 가이드 2.2 블루 모드는 "검정 위 블루". 장문은 밝은 바탕에서 읽힌다. 한 사이트 한 바탕은 관공서 팔레트가 된다 | 가이드 v5 |
| 오렌지 `#FA7000`은 채움·잉크 위 글자에 쓴다. 근백색 위 작은 글자는 `#B93A08`(5.38:1) | 근백색 위 `#FA7000`은 2.68:1이라 작은 글자로 못 쓴다 (plan-10) | — |
| **로고는 페이지당 최소 한 번 뷰포트 폭 60% 이상으로** 등장한다. 헤더 소형 배치는 하지 않는다 | 36px 로고는 실루엣만 남아 "각진 블록"으로 읽혔다. 로고 옆 수평 직선은 두지 않는다 | 로고 벡터 확보 시 크기 상한 재검토(현재 PNG 738px, 1.6배 확대) |
| 형태 앵커 3개 — 둥근 통통한 매스 / 손맛과 상승 기울기 / 단색 고대비 — 는 **로고·노드·버튼에** 적용한다. 컨테이너는 라운드 12 · 테두리 1px 헤어라인까지 (plan-10, 팀장이 고른 보드) | "둥근 매스"를 상자에 발랐더니 스티커북이 됐다(AI Council 4/4) | — |
| 기울기는 로고가 가진 것(약 8°)으로 충분하다. 레이아웃은 들여쓰기·크롭·비대칭으로 리듬을 만든다. 요소 회전은 ±3° 미만이면 하지 않는다 | ±1°는 의도가 아니라 오차로 읽힌다 | — |
| 타이포는 **극단**으로: 디스플레이 `clamp(44px, 7vw, 112px)` 800, 본문 18, 라벨 13. 중간 크기를 늘리지 않는다 | 가이드 2.3 "아주 크거나, 숨을 쉬거나" | — |
| 모션은 transform·opacity만, 200~900ms, `prefers-reduced-motion` 시 정지. 첫 화면 포함 | 0KB. 정지한 형태는 다이어그램, 움직이는 형태는 살아 있는 것 | 가이드 v5 모션 규정 |
| 국문 제목 `keep-all`, `-0.03em` | 어절 단위 줄바꿈, 대형 제목 자간 | — |

## 콘텐츠 기준

| 기준 | 이유 |
|---|---|
| 없는 사례·수치·후기는 만들어 채우지 않는다 | 채우는 순간 이 사이트 전체가 거짓말이 된다 |
| 모든 수치에는 표본 수, 측정 시점, 측정 방법을 붙인다 | 눈금 없는 수치는 검증 불가 |
| 비어 있는 값은 비어 있는 채로 둔다. 다만 자리표시 `[ ]`는 공개 페이지에 두지 않는다 | 내부적으로는 정직하지만 외부인에게는 미완으로 읽힌다. 비어 있으면 요소 자체를 렌더링하지 않거나, 왜 비어 있는지 한 문장으로 밝힌다 |
| 노션 원문을 그대로 쓰는 문장(미션·비전·가치·분기 목표)은 노션이 바뀌면 사이트를 바꾼다 | 출처가 하나여야 어긋나지 않는다. 사이트에서 먼저 고치지 않는다 |

## 기술 기준

| 기준 | 이유 | 재검토 조건 |
|---|---|---|
| 3D / WebGL / Spline은 첫 화면 밖에서 lazy-load하고, JS 예산 안에 들고, reduced-motion 시 정적 대체가 있을 때 쓴다 | 히어로에 넣으면 JS 800KB~2MB로 LCP 2.5s를 넘긴다 | 390px 실기기에서 FPS 30 이상, LCP 2.5s 이하로 측정되면 첫 화면 사용도 재검토 |
| `backdrop-filter`는 고정 요소(헤더 등) 한 곳까지 | 스크롤 컨테이너 안에서는 프레임마다 재합성돼 실사용 기기 FPS가 15~30% 떨어진다 | 실기기 측정에서 하락이 5% 미만이면 확대 |
| 부드러운 스크롤 라이브러리는 네이티브 `scroll-behavior`로 안 되는 연출이 생겼을 때 쓴다 | 그 전에는 라이브러리가 해결하는 문제가 없다. 번들만 는다 | 스크롤 연동 연출(관계 맵 등)이 결정될 때 |
| 390px에서 가로 스크롤이 생기지 않아야 한다 | 실사용 기기의 하한. 가로 스크롤은 레이아웃 실패의 가장 흔한 신호 | 없음 — 커밋마다 실측 |
| `prefers-reduced-motion`을 존중하고, 키보드 포커스를 표시하고, 텍스트 명암비 4.5:1 이상(24px+ 대형은 3:1) | WCAG 2.2 AA. 명암비는 상대휘도식으로 계산해 수치를 문서에 남긴다 | WCAG 갱신 시 |
| 첫 화면(로고 + 한 줄 + 필터)은 390×812에서 스크롤 없이 들어간다 | 첫 화면이 잘리면 로고를 보기 전에 떠난다 | 없음 |
| 예산: 폰트 합계 ≤300KB, LCP ≤2.5s, INP ≤200ms, 라이브러리 0이 기본 | 정적 원페이지에 프레임워크가 해결하는 문제가 없다 | 스크롤 연동 연출이나 상태 관리가 필요한 섹션이 생길 때 |
| 라이브러리를 쓰기로 했다면 이유·용량·대체 불가 사유를 `docs/plan-*.md`에 기록한다 | 기록 없는 의존성은 나중에 왜 있는지 아무도 모른다 | — |

## 현재 상태

로컬 구현 완료, 배포 대기. 2026-09-21 pencil-new.pen 구현(plan-11). 4페이지: `index`(사진 히어로 → What we do 3 → 프로젝트 4 → 밴드) · `projects`(필터) · `people` · `contact`(폼). 공통 `assets/site.css`·`site.js`

첫 화면은 팀 사진 풀블리드 + 눈썹 라벨. 로고 PNG는 이 라운드에서 미사용(팀장 시안이 텍스트 워드마크) — "로고 60%" 규칙은 보류, plan-11 §2.

### 최소 공개 단위 (URL 공유 조건)

① 정의문 ✅ ② 사례 1개 설명 ✅ ③ 팀 10명 이름 ✅ ④ 연락 수단 ✅ — 네 조건 충족, 2026-09-18 `noindex` 해제·공개.

### 미해결

- 로고 벡터 원본 미확보 — 웹 워드마크는 드라이브의 PNG 원본으로 반영됨(plan-07). 벡터는 인쇄·확대용으로 여전히 필요. 서체는 가이드 v4 원문 확인으로 Pretendard 확정
- 관계 맵 민감 2유형(부딪히기 쉬운·말이 엇갈리는) — 유형별 동의 시 확장
- 팀 섹션에 역할을 싣지 않는 이유 한 문장 (하단 안내문에 쓸 것)
- DATAFLOW 2 한 줄 설명, PLN 시작 시점
- "받지 않습니다" 문구 팀 합의
- 필터 전제 기각 조건 N = 3 (임시값, 팀이 바꿀 수 있음)
- 합격 기준 테스트(외부인 5명 10초) 실시

문서: `docs/structure.md`(IA v3) · `docs/decisions.md`(결정 기록) · `docs/plan-01~11`(섹션·감사·자산·재설계별 4칸 결정)
