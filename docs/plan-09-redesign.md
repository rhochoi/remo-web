# 계획 09 — 재설계 (2026-09-18)

계기: 팀장 "촌스럽다, 사람들 나갈 것 같다. 전면 재설계, 방해하는 규칙은 없애라."
절차: AI Council 4패널(디자인 디렉터 · 방문자 · 브랜드 수호자 · 악마의 변호인) 독립 의견 → 종합 → 한 번에 재작성. 회의록 요약은 [decisions.md](decisions.md).
형식: 결정 / 이유 / 근거 / 기각안.

## 0. 먼저 고정한 것 (네 번째 번복을 막기 위해 — 악마의 변호인 조건)

**결정 권한 순서** (위가 아래를 이긴다):
1. 콘텐츠 진실 규칙 · 접근성(WCAG AA) · 개인정보 동의 게이트 — 누구도 못 뒤집는다
2. 실측(390/1280 실기기, 명암비 계산, LCP)
3. 브랜드 가이드 v4 원문
4. 팀장 판단 — **"취향"이라고 적는다.** 근거 칸에 "팀 결정"이라고만 쓰지 않는다
5. AI Council 결론 — 권고이지 결정이 아니다

**합격 기준** (디자인 전에 씀): 외부인 5명에게 10초 노출 → "무엇을 하는 팀인가" 정답 ≥ 3/5, 노드를 만진 사람 ≥ 3/5, "촌스럽다" 0/5. 팀이 직접 돌린다.
**필터 전제 기각 조건의 N**: `3` (임시값 — 팀이 바꿀 수 있음. 2026-12-31까지 유효 문의 3건 미만이면 재검토).

## 1. 회의가 수렴한 진단

| 진단 | 근거(4패널 중) |
|---|---|
| 로고는 문제가 아니라 **쓰임**이 문제 — 36px, 수평 4px 헤더 선 옆, 밝은 바탕 | 4/4 |
| 촌스러움의 근원은 **"둥근 매스" 규칙의 균등 적용** — 4px 테두리 7곳, 라운드 36 전부, 중간값 타이포 | 디렉터 · 수호자 · 악마 |
| **블루 모드를 문자 그대로** 쓴 적이 없다 — 잉크 바탕 위 블루 로고 | 디렉터 · 수호자 |
| 첫 화면에 **보이는 한 줄**은 필요 — 단 헤드라인이 아니라 캡션 크기로 | 디렉터 · 방문자 |
| 디자인으로 못 고치는 것: 썸네일 0, 완료 사례 0, 역할 없는 10명, 로고 벡터 없음 | 악마 · 방문자 |

## 2. 결정

| 항목 | 결정 | 이유 | 근거 | 기각안 |
|---|---|---|---|---|
| 바탕 | 히어로·방향·푸터 **잉크**, 포트폴리오·팀·문의 근백색. 경계는 섹션 전환 자체 | 가이드 2.2 블루 모드 = 검정 위 블루. 장문(포폴·폼)은 밝은 바탕에서 읽힌다. 한 사이트 한 바탕이 관공서 팔레트를 만들었다 | 가이드 v4 2.2 · plan-07 §2 번복 | 전체 근백색(이전) — 브랜드가 얌전해짐 / 전체 잉크 — 폼·장문 가독 |
| 히어로 | 로고 폭 `min(92vw, 1180px)`, 잉크 위, 그 위에 관계 맵 노드 10개(젤리 볼륨) + 선(0.45 opacity). 아래 h1 22px/500 한 줄 + 필터 3개 텍스트 버튼 + 출처 12px | 로고가 화면의 주인공이 되면 "각짐"이 사라진다(수호자). 노드를 로고 위에 얹으면 "열 명이 맞물린 형태"가 로고와 한 몸(디렉터). h1은 방문자의 3초 요구를 캡션 크기로 충족 | 4패널 종합 | 텍스트 없는 맵(plan-08 §6) — 방문자 이탈 / 정의문 72px(plan-08 §3) — 로고와 경쟁 |
| 로고 | 원본 PNG 738×355 그대로(재생성 없음). 회전 추가 없음 — PNG에 이미 약 8° 상승 기울기 | 가이드 로고 규칙. 1180px 표시는 1.6배 확대 — 벡터 확보 전까지의 한계 | 실측 | AI 재생성 — 가이드 하드룰 4 위반 / 트레이싱 — 도구 없음 |
| 노드 | 지름 `r×1.7`(27~37px), radial-gradient + inset shadow로 젤리 볼륨, 10개 형태 다름, 홀수 노드 6~9초 부유 | 가이드 03장 소프트 폴(풍선/젤리)을 CSS만으로. 정지한 원은 다이어그램, 부유하는 볼륨은 살아 있는 것 | 가이드 03 · 2.5.8(≥24px) | 크롬 폴 — CSS로 흉내내면 싼티(디렉터) |
| 모션 | 로고 settle 900ms → 선 draw 700ms → 노드 pop 60ms 스태거 → 부유. 스크롤 시 로고 -12vh·0.94(scroll-driven, 지원 브라우저만). 전부 transform/opacity, reduced-motion 시 정지 | 첫 방문의 3초를 "부풀었다 자리잡는 풍선"으로. 0KB | 가이드 "위로 튀어 오르는 다이내믹" · README 예산 | 라이브러리 모션 — 예산 위반 |
| 헤더 | fixed, 히어로 위에서 투명·근백색 글자, 스크롤 후 반투명 근백색·잉크 글자. 로고 없음(히어로가 로고), 텍스트 "REMO" 13px. **직선 룰 없음** | 로고 옆 수평 4px 선이 로고를 "수평선에 안 맞는 물체"로 만들었다(수호자) | 수호자 진단 | 4px 선 헤더(이전) |
| 방향 | 매니페스토: 미션 `clamp(44px, 7vw, 112px)` 800, 계단 들여쓰기 1.2em × 2 / 비전 34px 500 / 가치는 한 줄 슬래시 / 분기 목표 22px. 카드·필·기울기 전부 삭제 | 가이드 2.3 "아주 크거나 숨을 쉬거나". 계단이 로고의 상승 리듬을 회전 없이 번역 | 디렉터 | 라운드 카드 3개(이전) — 스티커북 |
| 포트폴리오 | 인덱스 행 4개, 1px 헤어라인, 이름 `clamp(36px, 6vw, 88px)` 800, 메타 13px 우측. hover 시 행이 블루로 채워지고 근백색 글자 | 이미지가 없으면 타이포로 정직하게. 행 전체가 링크라 어디를 눌러도 노션 | 디렉터 · 콘텐츠 규칙 | 카드 4개(이전) — 썸네일 자리가 비어 보임 |
| 팀 | 사진 풀블리드 21:9 → "열 명" 13px 라벨 → 이름 10개를 `clamp(40px, 7vw, 104px)` 800 한 단락으로 | 균등 카드 10개는 가이드가 금지한 "얌전한 그리드". 이름이 크면 명단이 아니라 선언 | 디렉터 · 수호자 | 카드 + 이니셜 원(이전) |
| 문의 | 근백색, 밑줄 입력, 2열. **필수 3**(소속·문제·이메일), 나머지 선택. 제목 "문제를 적어 보내 주세요." 버튼은 사이트 유일한 알약(블루, 64px, 풀폭) | "필터할 만큼 문의가 올 때 필터가 성립한다. 지금은 필터 앞에 사람이 없다"(방문자). 시간을 들일 사람은 선택 칸도 채운다 | 방문자 · 악마 | 필수 7(이전 "무거운 전환") — 견적 요청서의 무게 |
| 푸터 | 잉크, 로고 폭 140% 아래·오른쪽 크롭 | 가이드 2.4 "과감한 크롭". 페이지가 로고로 시작해 로고로 끝난다 | 가이드 2.4 | 없음 |
| 라운드·획 | 컨테이너 라운드 0, 테두리 1px 헤어라인 또는 없음. 둥근 것은 로고·노드·보내기 버튼뿐 | "둥근 매스"는 로고와 채워진 덩어리의 성질이지 상자의 성질이 아니다 | 4패널 | 20px+ 라운드 전면(이전 README) |
| 오렌지 | 여전히 미사용 | 한 화면 한 모드. 오렌지 on 근백색 2.84:1이라 텍스트 불가, 그래픽 자산도 없음 | 가이드 하드룰 2 · 명암 | 악마 "진작 없앴어야 할 규칙" — 자산이 생기면 재검토 |

## 3. 명암 (계산)

근백색 on 잉크 18.6 · `#A8A8A4` on 잉크 8.9 · 근백색 on 블루 7.31 · `#E3ECF5` on 블루 6.51 · `#5A5A5A` on 근백색 6.49 · 블루 로고 on 잉크 2.55(비텍스트 브랜드 그래픽 — 가이드 블루 모드 그 자체).

## 4. 검증 (2026-09-18)

375: 첫 화면 812 안, 가로 넘침 0, 노드 최소 27px, 미션 44px, 이름 40px. 데스크톱(825 패널): 로고 759px, 콘솔 오류 0. 1280은 배포 후 확인.

## 5. 재설계로도 남는 것 (정직하게)

프로젝트 이미지 0 · 완료 사례 0 · DATAFLOW 2 설명 없음 · 역할 없는 10명 · 로고 벡터 없음 · 뒷모습 사진 1장. 이 여섯은 **자산 스프린트**의 몫이지 CSS의 몫이 아니다.
