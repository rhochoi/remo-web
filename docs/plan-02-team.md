# 계획 02 — 팀 섹션

형식: 결정 / 이유 / 근거 / 기각안. 공통 토큰은 [plan-01](plan-01-header-direction.md)을 따른다.

## 데이터 상태 (2026-09-17)

| 항목 | 상태 |
|---|---|
| 표기명 10 | 확정 — 브루노 / 쏠 / 조이 / 정우 / 막스 / 유진 / 로 / 겸 / 이든 / 비크 |
| 포지션 | 미제출 → **렌더링 안 함** |
| 담당 한 줄 | 미제출 → 렌더링 안 함 |
| 개인 링크 | 미제출 → 렌더링 안 함 |
| 프로필 이미지 | 미제출 → 이니셜 원형 |

## 결정

| 항목 | 결정 | 이유 | 근거 | 기각안 |
|---|---|---|---|---|
| 섹션 번호 | 아이브로 "02 — 팀" (structure.md 인덱스는 4) | 아이브로 번호는 방문자의 위치 단서다. 렌더링된 순서를 따라야 하고, 02·03이 빠진 채 "04"가 보이면 깨진 페이지로 읽힌다 | 위치 단서의 목적 | structure.md 인덱스 그대로 "04" — 내부 문서 번호를 방문자에게 노출할 이유 없음 |
| 제목 | h2 "리모의 열 명" | 사실만. 캔버스 시안의 "열 명이 각자 맡은 것"은 포지션이 없는 지금 지킬 수 없는 약속 | 콘텐츠 규칙 "없는 것을 채우지 않는다" | "열 명이 각자 맡은 것" — 포지션 제출 후 재검토 |
| 빈 값 처리 | 포지션·담당·링크 요소를 **DOM에 두지 않음**. 섹션 하단에 한 줄: "포지션과 담당은 팀원 본인이 제출한 것만 싣는다. 아직 제출 전이다." | 자리표시 `[ ]`는 공개 페이지에서 미완으로 읽힌다(AI Council). 대신 왜 비어 있는지를 문장으로 밝히는 것이 "눈금 붙은 수치" 원칙의 문장판 | structure.md §4 "미제출 항목은 비운다" · 최소 공개 단위 | `[포지션]` 자리표시 — 캔버스 시안용. 공개 페이지 부적합 |
| 이니셜 원형 | 64px(모바일 48px), 이름 첫 글자, 900 | 이미지 미제출 시 대안. 64는 8배수이고 이름 24px과 1:2.67 대비로 카드 안 시각 앵커가 됨 | 8pt 격자 · 위계 | 아이콘/실루엣 — 익명 아바타는 "사람 없음"으로 읽힘 |
| 원형 색 | 기본 잉크, 1·4·7·10번째만 블루 | 단일 지배색 + 절제된 액센트. 3주기는 2주기(체스판)보다 규칙이 덜 보여 손맛에 가깝다 | 디자인 정책 "단일 지배색·절제된 액센트" | 전부 잉크 — 10개가 같으면 눈이 미끄러짐 / 교대 — 체스판 패턴 |
| 카드 | 4px 잉크 아웃라인, 라운드 28, padding 24, 폭 데스크톱 232 / 모바일 `calc(50% - 6px)` | 232×4 + 24×3 = 1000 < 1120(1280−80×2) → 4+4+2로 랩되며 마지막 줄이 비어 균등 그리드가 깨짐. 모바일은 2열 | README 디자인 기준(라운드 ≥20, 획 ≥3, 균등 그리드는 기본값 아님) | CSS grid 5열 — 10개가 딱 맞아 균등 그리드가 됨 |
| 카드 상단 오프셋 | nth-child별 0 / 24 / 0 / 40 / 12 / 0 / 32 / 0 / 16 / 40 px. 모바일은 절반 | 이 섹션의 "손맛" 제스처. 전부 8배수, 최대 40 < 카드 간격 24×2 → 한 묶음으로 유지되면서 격자가 깨짐 | 형태 앵커 "손맛" · 게슈탈트 근접 | 기울기 — plan-01 원칙 "화면당 강한 제스처 1개". 이 섹션은 오프셋이 그 1개 |
| 상호작용 | 없음. 카드 호버·클릭 없음 | 링크가 없는 카드에 어포던스를 주면 거짓 신호 | plan-01 "호버 없음" 결정과 동일 이유 | 카드 전체 클릭 → 프로필 — 프로필이 없음 |
| 내비 | 헤더에 "팀" 링크 추가 | 섹션이 실제로 존재하게 됨 | plan-01 "존재하는 섹션만" | — |
| 순서 | 사용자가 준 순서 그대로 | 순서에 의미를 부여할 근거(가나다·입사순·역할)가 없다 | — | 가나다순 — 근거 없는 재정렬 |

## 포지션 제출 후 바뀌는 것

- 카드에 포지션(15px 700 블루) + 담당 한 줄(15px 캡션색) 추가
- h2를 "열 명이 각자 맡은 것"으로 재검토
- 하단 안내 문장 제거
- 링크는 본인이 공개한 것만, 텍스트 링크로(아이콘 세트 도입 안 함 — 3개 이하면 이름이 더 명확)
