# 계획 10 — V10 (2026-09-19)

계기: 팀장이 보낸 블루프린트 보드 2장("REMO V10 Experience Blueprint" · "Developer Interaction Handoff")을 "참고해서 만들어줘".
보드의 **콘텐츠**(행사·세션·참여자 플랫폼)는 리모의 사실이 아니므로 쓰지 않는다. 가져온 것은 **시각 언어**뿐이다.
형식: 결정 / 이유 / 근거 / 기각안.

## 1. 보드에서 가져온 것 · 안 가져온 것

| 가져옴 | 안 가져옴(이유) |
|---|---|
| 키커 라벨(오렌지 대문자 `01 / SECTION`) · 800 굵기 제목 · 흰 카드 1px 헤어라인 라운드 12 · 번호 카드 · 첫 칸만 오렌지로 채운 단계 행 · 알약 CTA 2개 · 잉크/근백색 교대 · 사람 카드 | 세션·연사·참여자 수, 라우트 맵, 저장/대화 요청 플로우(리모에 없는 기능·수치 — 콘텐츠 진실 규칙) · Funnel Sans(국문 없음 → Pretendard 유지) · 보드의 잉크 `#151515`(가이드 `#0A0A0A` 유지) |

## 2. 결정

| 항목 | 결정 | 이유 | 근거 | 기각안 |
|---|---|---|---|---|
| 오렌지 복귀 | `#FA7000`은 **채움과 잉크 위 글자**에, 근백색 위 작은 글자는 `#B93A08` | 보드의 정체성이 오렌지 라벨이다. 다만 `#FA7000` on 근백색은 2.68이라 작은 글자로 못 쓴다 — 접근성이 취향보다 위 | 명암 계산 §3 · 결정 권한 순서 1 > 4 | 보드 값 `#FF5A1F` 그대로(2.88, 실패) / 오렌지 미사용 유지(보드와 다른 사이트) |
| 히어로 | 잉크, 로고 7 : 글 5 2열. 태그 → h1(30~44px 800) → 증거 한 줄 → 알약 2개(하는 일 보기 / 문제 보내기) | 보드 원칙 01 "첫 화면은 두 행동만" · 02 "증명은 CTA 옆에" | 보드 Landing 명세 | 로고 + 한 줄만(plan-09 §6) — 다음 행동이 없었다 |
| 방향 | 원칙 카드 3(미션·비전·분기 목표) + 가치 5 단계 행(첫 칸 오렌지) | 보드 Principles·Journey 구조. 노션에 실제로 있는 문장만 | structure.md §방향 | 매니페스토 대형 타이포(plan-09) — 팀장 "글이 너무 많다" |
| 프로젝트 | 잉크 위 카드 4(2열). 기간·참여자 → 이름 → 한 줄 → 현재 단계 | 보드 Session card. DATAFLOW 2는 설명이 없으니 설명 칸을 렌더링하지 않는다 | 콘텐츠 규칙 "비어 있으면 요소를 그리지 않는다" | 인덱스 행(plan-09) |
| 팀 | 사진 카드 + 이름 카드 10(5열/2열), 번호·오렌지 점. 역할 없음 | 보드 People card. 역할은 팀 결정으로 비움 | 팀 결정 | 이름의 벽(plan-09) |
| 문의 | 좌 스펙 카드 4(필수 3·받은 뒤·보관·다른 길) + 우 흰 카드 폼. 필수 3 유지, 선택 라디오는 알약 칩 | 보드 Spec card + Landing Specifications 형식 | plan-09 문의 결정 유지 | — |
| 컨테이너 라운드 | 12px 허용(plan-09 "라운드 0" 개정) | 보드의 카드가 12px. 20px+ 전면 라운드(스티커북)와는 다르다 | 취향(팀장이 고른 보드) | 0 유지 — 보드와 어긋남 |

## 3. 명암 (계산, 2026-09-19)

`#FA7000` on `#0A0A0A` 6.96 · 잉크 on `#FA7000` 6.42 · `#B93A08` on `#F7F6F2` 5.38 · `#5A5A5A` on 근백색 6.49 · `#C1C0BB` on `#1D1D1B` 9.27 · 보드 원값 `#FF5A1F` on 근백색 2.88(작은 글자 불가 → 미채택).

## 4. 검증 (2026-09-19, 로컬 8765)

1280: 히어로 502px, h1 44px, 콘솔 오류 0. 390: 히어로 하단 651 < 812, 가로 넘침 0, h1 30px, CTA 풀폭 스택.

## 5. 남는 것

보드가 전제한 "이미지"는 여전히 없다(프로젝트 썸네일 0, 로고 벡터 없음). 카드는 타이포로 버틴다. 로고 벡터·프로젝트 화면이 오면 히어로 우측과 프로젝트 카드 상단에 자리가 있다.
